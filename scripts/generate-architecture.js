#!/usr/bin/env node
// Retired fail-closed writer. Do not use.
//
// Architecture docs now live under architecture/ and are maintained by explicit
// story-reviewed edits plus deterministic inventory generators.

process.stderr.write(
  'generate-architecture.js is retired and fail closed. Edit architecture/architecture.md through an approved story instead.\n',
);
process.exit(1);
