# lib/ — most code is tracked; specific user-specific migrations are local.
migrations/024_topic_hierarchy.sql
migrations/025_topic_sort_order.sql

# st_5a63545d — cohort build stamp is generated by scripts/build/stamp-cohort.js
# at release time. Never committed; the public key (cohort/public-key.pem)
# IS committed because clients need it to verify the JWT.
cohort/build-info.js
